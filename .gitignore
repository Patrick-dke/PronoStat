# Secrets — ne jamais publier
.env
.streamlit/secrets.toml

# Cache local (cotes, forme, effectifs, quotas, historique)
.cache/

# Artefacts du deploiement Firebase (regeneres a chaque publication)
.firebase/
firebase-debug.log
*-debug.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
*.egg-info/

# Éditeurs / OS
.vscode/
.idea/
.claude/settings.local.json
.DS_Store
Thumbs.db

# Depot git vide cree par erreur
PronoStat/

# Workflows n8n generes (outils de construction, pas des livrables)
outputs/_*.py
outputs/_*.js

# Donnees publiees avec l interface (regenerees par tools/publier_donnees.py)
web/data.json
