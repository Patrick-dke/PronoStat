# Dépendances de production. L'hébergeur n'installe que ce fichier.
#
# Les bornes hautes (<2, <3, <7) sont volontaires : elles empêchent qu'une
# future version majeure, publiée après votre mise en ligne, casse
# l'application sans prévenir lors d'un redéploiement.
streamlit>=1.36,<2
requests>=2.31,<3
numpy>=1.26,<3
pandas>=2.1,<3
plotly>=5.20,<7
python-dotenv>=1.0,<2
# Signature des jetons pour Firestore, où le journal des pronostics survit
# aux redémarrages. Volontairement `google-auth` seul, et non la bibliothèque
# cliente officielle : celle-ci entraîne gRPC et protobuf, une cinquantaine
# de mégaoctets qui alourdiraient chaque déploiement pour une lecture et une
# écriture par opération. Sans compte de service configuré, cette dépendance
# ne sert simplement à rien et l'application garde son fichier local.
google-auth>=2.28,<3
